import {  } from "./index";

describe("index", () => {
  describe("addCube", () => {
    it("should add initial cube to scan", () => {
      expect.assertions(1);
      // const res = addCube([1, 1, 1]);
      // expect(res.data.length).toBe(1);
    });
  });
});
